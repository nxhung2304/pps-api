## **Status:**
- Review: Approved
- PR: Done

## Metadata
- **Title:** Stats Model & Schema
- **Phase:** 3: Stats Aggregation & Querying
- **GitHub Issue:** #13

---

## Description
Implement the `Stat` model to store pre-computed daily metrics for users. This table will be updated by the `stats_processor` consumer to allow for efficient querying of user progress over time without scanning raw events.

---

## Acceptance Criteria
- [x] `Stat` model is created with fields: `user_id`, `date`, `total_coding_time`, `total_gym_time`, `total_run_distance`, `sleep_duration`, `event_count`.
- [x] Migration includes a unique index on `[:user_id, :date]`.
- [x] Model includes validations for field presence and numericality.
- [x] Atomic upsert logic (e.g., `upsert_all` or custom increment method) is implemented and verified.
- [x] Unit tests cover metrics aggregation and unique constraint handling.

---

## Implementation Checklist
- [x] Run `rails generate model Stat user:references date:date total_coding_time:integer total_gym_time:integer total_run_distance:float sleep_duration:float event_count:integer`.
- [x] Update the migration file to add `null: false` constraints, and add the unique index.
- [x] Add default values (0) for all metric columns in the migration.
- [x] Implement a class method `Stat.increment_metrics(user_id, date, metrics_hash)` that uses an atomic upsert.
- [x] Add unit tests in `test/models/stat_test.rb`.

---

## Step-by-step Guide

**Files to create/modify:**
- `app/models/stat.rb`: Define the model, validations, and the `increment_metrics` method.
- `db/migrate/YYYYMMDDHHMMSS_create_stats.rb`: Migration with unique index and defaults.
- `test/models/stat_test.rb`: Test for unique index and increment logic.

**Key decisions:**
- Use `insert_all` with `unique_by: [:user_id, :date]` or `upsert` (Rails 7+) for atomic updates.
- Note: `upsert` might overwrite instead of incrementing. A custom SQL query with `ON CONFLICT (user_id, date) DO UPDATE SET total_coding_time = total_coding_time + EXCLUDED.total_coding_time` is preferred for true atomicity.

**Non-obvious snippets** *(only if needed)*:
```ruby
# Example of atomic increment via SQL
def self.increment_metrics(user_id, date, updates)
  # updates = { total_coding_time: 120, event_count: 1 }
  # Use raw SQL or a gem to handle atomic increments on conflict
end
```

---

## Notes
- Depends on the `User` model.
- Essential for Phase 3.2 (Consumer) and 3.3 (Query API).
- Ensure `date` is treated correctly across timezones (usually UTC date).
