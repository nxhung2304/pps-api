## **Status:**
- Review: Approved
- PR: Draft

## Metadata
- **Title:** Secure API Base & Event Model
- **Phase:** 1.3
- GitHub Issue: #6

---

## Description
Establish core security for API controllers and implement the Event tracking model, adhering to the PRD schema.

---

## Acceptance Criteria
- [x] `ApplicationController` has `authenticate_user!` method implemented using `JwtService`.
- [x] `Event` model created with `id` (UUID), `user_id` (UUID, reference), `type` (VARCHAR), `timestamp` (BIGINT), and `payload` (JSONB) fields.
- [x] Validations present for `type` (whitelist) and `timestamp` (not in future).
- [x] Migration includes required indexes: `(user_id, type, timestamp)` and `(user_id, timestamp)`.
- [x] Unit tests for `Event` model exist.

---

## Implementation Checklist
- [x] Modify `app/controllers/application_controller.rb` to implement `authenticate_user!` using `JwtService`.
- [x] Generate migration `CreateEvents` with defined schema and indexes.
- [x] Create `app/models/event.rb` with validations.
- [x] Write `test/models/event_test.rb`.


---

## Step-by-step Guide

**Files to create/modify:**
- `app/controllers/application_controller.rb` — add `authenticate_user!` leveraging `JwtService.decode`.
- `db/migrate/[timestamp]_create_events.rb` — implement schema and indexes.
- `app/models/event.rb` — define `belongs_to :user` and validations.
- `test/models/event_test.rb` — test schema constraints and validations.

**Key decisions:**
- `timestamp` is stored as `BIGINT` (Unix epoch).
- `payload` is `JSONB` with a default of `{}`.
- Indexes on `(user_id, type, timestamp)` and `(user_id, timestamp)` for query performance.

---

## Notes
- Validation logic at the API layer (in controller/service) must prevent invalid payloads from reaching Kafka.
- Ensure `timestamp` validation rejects future dates.
